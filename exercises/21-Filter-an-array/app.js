let allNames = ["Romario", "Boby", "Roosevelt", "Emiliy", "Michael", "Greta", "Patricia", "Danzalee"];

// Your code here
let resultingNames = allNames.filter(function(names){
    return (names[0] === 'R' || names[0] === 'r') 
    }
)
console.log(resultingNames);
